[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP server for searching Google News RSS, top stories, decoded article URLs, and extracted article text."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.11.18",
    "google-news-api",
    "mcp[cli]>=1.6.0,<2.0.0",
    "trafilatura>=2.0.0",
]

[tool.uv.sources]
google-news-api = { workspace = true }
